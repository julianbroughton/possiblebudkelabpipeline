image:
  file: Dockerfile

tasks:
  - init: 'echo "Preparing your Gitpod environment..."'
  - command: 'echo "Ready to run Nextflow pipelines!"'

ports:
  - port: 8080
    onOpen: open-preview

vscode:
  extensions:
    - ms-kubernetes-tools.vscode-kubernetes-tools
